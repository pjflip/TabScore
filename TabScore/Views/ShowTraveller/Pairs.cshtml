<!-- TabScore - TabScore, a wireless bridge scoring program.  Copyright(C) 2023 by Peter Flippant
Licensed under the Apache License, Version 2.0; you may not use this file except in compliance with the License -->

@model TabScore.Models.Traveller
@using Resources

<div class="row mt-4">
    @if (Model.HandRecord)
    {
        <div class="col-9">
            <div class="row">
                <div class="col-12 text-center">
                    <h3>
                        @Strings.Board @Model.BoardNumber
                    </h3>
                </div>
            </div>
            <div class="row">
                <div class="col-12 text-center">
                    @if (Model.PercentageNS == "")
                    {
                        <h4>
                            @Strings.Traveller
                        </h4>
                    }
                    else
                    {
                        <h4>
                            @Strings.N@Strings.S: @Model.PercentageNS - @Strings.E@Strings.W: @Model.PercentageEW
                        </h4>
                    }
                </div>
            </div>
        </div>
        <div class="col-3">
            <button class="btn btn-outline-light btn-lg mr-2 my-2 float-right" onclick="handRecordClick()">
                <img src="~/Resources/Cards Logo.png" />
            </button>
        </div>
    }
    else
    {
        <div class="col-12 text-center">
            <h4>
                @Strings.Board @Model.BoardNumber
            </h4>
        </div>
        <div class="col-12 text-center">
            @if (Model.PercentageNS == "")
            {
                <h4>
                    @Strings.Traveller
                </h4>
            }
            else
            {
                <h4>
                    @Strings.N@Strings.S: @Model.PercentageNS - @Strings.E@Strings.W: @Model.PercentageEW
                </h4>
            }
        </div>
    }
</div>

<div class="row">
    <div class="col-12">
        <table class="table-sm table-bordered text-center" style="margin:auto; width:50%">
            <thead class="thead-dark">
                <tr>
                    <th>@Strings.N@Strings.S</th>
                    <th>@Strings.E@Strings.W</th>
                    <th>@Strings.Contract</th>
                    <th>@Strings.ByOnTraveller</th>
                    <th>@Strings.Lead</th>
                    <th>@Strings.Tricks</th>
                    <th>@Strings.N@Strings.S @Strings.Score</th>
                    <th>@Strings.E@Strings.W @Strings.Score</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var Result in Model)
                {
                   if (Result.Highlight)
                    {
                        <tr class="table-success">
                            <td>@Result.NumberNorth</td>
                            <td>@Result.NumberEast</td>
                            <td>@Html.Raw(Result.Contract)</td>
                            <td>@Result.DeclarerNSEW</td>
                            <td>@Html.Raw(Result.LeadCard)</td>
                            <td>@Result.TricksTaken</td>
                            <td>@Html.Raw(Result.ScoreNS)</td>
                            <td>@Html.Raw(Result.ScoreEW)</td>
                        </tr>
                    }
                    else
                    {
                        <tr>
                            <td>@Result.NumberNorth</td>
                            <td>@Result.NumberEast</td>
                            <td>@Html.Raw(Result.Contract)</td>
                            <td>@Result.DeclarerNSEW</td>
                            <td>@Html.Raw(Result.LeadCard)</td>
                            <td>@Result.TricksTaken</td>
                            <td>@Html.Raw(Result.ScoreNS)</td>
                            <td>@Html.Raw(Result.ScoreEW)</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>

<script>
    var isSubmitted = false;

    $(document).on('click', '#OKButton:enabled', function () {
        if (!isSubmitted) {
            isSubmitted = true;
            location.href = '@Url.Action("Index", "ShowBoards")?tabletDeviceNumber=@Model.TabletDeviceNumber';
        }
    });

    function handRecordClick() {
        if (!isSubmitted) {
            isSubmitted = true;
            location.href = '@Url.Action("Index", "ShowHandRecord")?tabletDeviceNumber=@Model.TabletDeviceNumber&boardNumber=@Model.BoardNumber&fromView=@Model.FromView.ToString().ToLower()';
        }
    }

    $(document).on('click', '#BackButton:enabled', function () {
        if (!isSubmitted) {
            isSubmitted = true;
            location.href = '@Url.Action("Index", "ConfirmResult")?tabletDeviceNumber=@Model.TabletDeviceNumber';
        }
    });
</script>

